# Palette's Journal

## 2025-05-15 - Icon-Only Button Accessibility Pattern
**Learning:** Key modal and panel components use icon-only buttons (X, Chevron) without `aria-label`, relying solely on visual cues.
**Action:** Systematically audit all `lucide-react` icon usage in buttons and enforce `aria-label` or `sr-only` text.
