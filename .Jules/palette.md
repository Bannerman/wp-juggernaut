## 2026-04-20 - Adding ARIA labels to icon-only buttons
**Learning:** Icon-only buttons lacking accessible names are a common accessibility issue across multiple components (e.g., modals, layout editors). Screen readers cannot interpret what these buttons do without labels.
**Action:** Add descriptive `aria-label` attributes to all icon-only buttons to ensure keyboard and screen reader accessibility without affecting visual design.
